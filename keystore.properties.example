# Copy to keystore.properties (gitignored) and fill in.
# Paths are resolved against the repository root, so a bare filename means
# a .jks sitting next to this file.
storeFile=foldwall-release.jks
storePassword=
keyAlias=foldwall
keyPassword=
